import styled from "styled-components";
import { Formulario } from "../../styles";

export const FormModal = styled(Formulario)`
  background: var(--grey3);
  padding: 8px;
  text-align: center;
  margin: 0px auto;
  box-shadow: none;
  max-width: 100%;
  width: 100%;
`;
